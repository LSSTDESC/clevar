#!/usr/bin/env python
import warnings

from .footprint import Footprint
from ..utils import hp

"""
class Masks():
    def __init__(self, config):
        ftpt_conf1 = config['footprint1'] if 'footprint1' in config else None
        self.ftpt1 = None
        if 'footprint1' in config:
            self.ftpt1 = FootprintZmax.load_external()
        self.cat1 = cat1
        self.cat2 =
    def _load_ft(self, ftpt_conf):
        if 'nest' not in ftpt_conf:
            warnings.warn("Footprint key nest not provided, 'False' used")
        ftpt = FootprintZmax.load_external(ftpt_conf['file'], ftpt_conf['nside'])
"""
